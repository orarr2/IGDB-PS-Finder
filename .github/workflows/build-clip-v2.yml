name: Rebuild CLIP v2

# Re-embeds game screenshots with jina-clip-v2 (1024-d) through the photo-search
# edge-function proxy, and commits the vectors (src/ml/clip_v2/).

# Manual-only: dispatch this after the Jina account has balance. Runs the
# resumable rebuild (src/ml/clip_v2.py), which continues from whatever is already
# committed and checkpoints progress as it goes.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  rebuild:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install requests pillow
      - name: Embed via proxy
        env:
          SHOTS: "4"
        run: |
          export SUPABASE_URL=$(grep -oP 'SUPABASE_URL:\s*"\K[^"]+' src/docs/config.js)
          export SUPABASE_KEY=$(grep -oP 'SUPABASE_KEY:\s*"\K[^"]+' src/docs/config.js)
          python src/ml/clip_v2.py
      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/ml/clip_v2
          git commit -m "Add jina-clip-v2 screenshot embeddings [skip ci]" || { echo "no changes"; exit 0; }
          git push
      - uses: actions/upload-artifact@v4
        with:
          name: clip-v2
          path: src/ml/clip_v2/*.json
          if-no-files-found: warn
