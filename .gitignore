# IGDB dataset output (large; regenerated by the notebook)
igdb_dataset/
*.parquet
*.csv

# Secrets / credentials — never commit
.env
*.env
credentials.json

# Node (PWA test harness)
node_modules/
package-lock.json

# Android TWA build outputs (generated in CI; never commit keys)
android/*.keystore
android/*.apk
android/*.aab
android/app/
android/.gradle/
android/build/
android/gradle*
android/store_icon.png
android/twa-manifest.json.bak

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
