# Project notes for Claude

## Writing style (STRICT)
- NEVER use an em-dash (the long dash) or an en-dash anywhere: not in chat replies,
  not in code, comments, commit messages, UI copy, docs, or any artifact.
- Use a single hyphen "-" instead. One hyphen is always enough.
- This applies to every response and every file, always.
